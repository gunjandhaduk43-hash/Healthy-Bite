# Database Setup

1. Start MySQL from XAMPP.
2. Open `http://localhost/phpmyadmin`.
3. Import `schema/001_stage_1.sql`.
4. Open the Healthy Bite registration page to create the first owner and restaurant.

The first schema intentionally includes only Stage 1 authentication and restaurant-profile tables. Later stages add menu, QR, and ordering tables through new numbered schema files.
